<h1>Annoyance's bookings</h1>

    <ul>
      <% @bookings.each do |booking| %>
        <li> <%= link_to booking.status, new_booking_path %> </li>
        <% end %>
    </ul>

<%= link_to "Book an annoyance", new_booking_path %>
